#include <iostream>
#include <cstdlib>
#include <string>
#include <limits>
#include <vector>

using namespace std;



int main(int argc, char** argv) {
    cout << "ok" << endl;


    // Multi Dimensional arr -- layers, columns, rows
    int arr[2][2][2] = 
        {   
            {
                {1,2}, {4,3}
            }
        };

    // Vectors (basically dyanmic array)
    vector<int> v(2);

    // stringstream grab strings separated by a space 

    return 0;
}